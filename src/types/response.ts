export type Category = {
  categoryId: number;
  name: string;
};
export type categoryId = {
  question: string;
  firstOption: string;
  secondOption: string;
  thirdOption: string;
  answer: string;
}[];
