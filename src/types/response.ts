export type Category = {
  categoryId: number;
  name: string;
};
export type categoryId = {
  question: string;
  firstOption: string;
  secondOption: string;
  thirdOption: string;
  answer: string;
}[];

export type statistics = {
  question: string;
  selectedAnswer: string;
  answer: string;
};
